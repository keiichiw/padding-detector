name: padding-detector
version: "1.0"
about: Detects implicit paddings in C structs and unions.
args:
    - INPUT:
        help: Sets the input header file to check
        required: true
        index: 1
    - OUTDIR:
        short: o
        long: outdir
        value_name: DIR
        help: Set a directory to store generated code.
        takes_value: true
